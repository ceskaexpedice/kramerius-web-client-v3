/**
 * Sentinel option value for the CDK source selector's "All sources" entry.
 * When selected, no `cdk.collection` filter is applied — the aggregated view
 * across every member library is shown. Only the periodical page's selector
 * offers this option (via MetadataSection's `includeAllOption`).
 */
export const ALL_SOURCES = '__all__';
